import styled from 'styled-components';

import { colors } from 'variables';

const Container = styled.h3`
  color: ${colors.CONCRETE_GRAY};
  font-size: 23px;
  text-align: center;
  font-weight: 300;
  font-style: italic;
  font-family: serif;
`;

export default Container;
